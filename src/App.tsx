import { Header } from '@modules/Header';
import './App.scss';
import { Slider } from '@modules/Slider';

export const App = () => {
  return (
    <div className="App">
      <Header />
      <Slider />
    </div>
  );
};
